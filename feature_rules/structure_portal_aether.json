{
		"format_version": "1.13.0",
		
		"minecraft:feature_rules": {
				"description": {
						"identifier": "farlands:structure_portal_aether",
						"places_feature": "farlands:structure/structure_portal_aether_aggregate"
				},
				
				"conditions": {
						"placement_pass": "after_surface_pass"
				},
				
				"distribution": {
						"iterations": "
							v.target_x = 105622;
							v.target_z = 105622;
							v.target_y = 256;
							
							v.chunk_x = math.mod(v.target_x, 16) + 16 * (v.target_x < 0);
							v.chunk_z = math.mod(v.target_z, 16) + 16 * (v.target_z < 0);
							
							return
								v.originx == 16 * math.floor(v.target_x / 16)
								&& v.originz == 16 * math.floor(v.target_z / 16)
							;
						",
						
						"x": "v.chunk_x",
						"z": "v.chunk_z",
						"y": "v.target_y"
				}
		}
}